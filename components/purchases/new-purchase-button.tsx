"use client";
import { useState } from "react";
import { quickPurchase } from "@/app/(dashboard)/compras/actions";
import { Icon } from "@/components/ui/icons";
type Product={id:string;name:string;base_unit:string};
export default function NewPurchaseButton({products}:{products:Product[]}){const[open,setOpen]=useState(false);async function handle(formData:FormData){await quickPurchase(formData);setOpen(false)}return <><button onClick={()=>setOpen(true)} className="pt-button-primary inline-flex items-center gap-2 px-4 py-2 text-sm"><Icon name="plus" className="h-4 w-4"/>Nueva compra</button>{open&&<div className="fixed inset-0 z-[80] flex items-end bg-[#3e2833]/25 sm:items-center sm:justify-center sm:p-4"><div className="w-full rounded-t-[24px] bg-white p-5 sm:max-w-lg sm:rounded-[24px]"><div className="flex items-start justify-between"><div><p className="text-xs font-bold uppercase tracking-wide text-[#b54776]">Ingreso de mercadería</p><h3 className="mt-1 text-xl font-bold text-[#3e2833]">Nueva compra</h3></div><button onClick={()=>setOpen(false)} className="h-9 w-9 rounded-xl bg-[#fff1f7] text-lg">×</button></div><form action={handle} className="mt-5 space-y-4"><Field label="Proveedor"><input className="pt-input" name="supplier" required placeholder="Nombre del proveedor"/></Field><Field label="Producto"><select className="pt-input" name="product_id" required defaultValue=""><option value="" disabled>Elegir producto…</option>{products.map(p=><option key={p.id} value={p.id}>{p.name} ({p.base_unit})</option>)}</select></Field><div className="grid grid-cols-2 gap-3"><Field label="Cantidad base"><input className="pt-input" type="number" min="0.001" step="0.001" name="quantity_base" required/></Field><Field label="Costo por unidad base"><input className="pt-input" type="number" min="0" step="0.0001" name="unit_cost" required/></Field></div><Field label="Nota"><textarea className="pt-input min-h-20 resize-none" name="notes"/></Field><div className="flex gap-2"><button type="button" onClick={()=>setOpen(false)} className="pt-button-secondary flex-1 px-4">Cancelar</button><button className="pt-button-primary flex-1 px-4">Recibir compra</button></div></form></div></div>}</>}
function Field({label,children}:{label:string;children:React.ReactNode}){return <label className="block"><span className="mb-1.5 block text-[10px] font-bold uppercase tracking-wide text-[#80616f]">{label}</span>{children}</label>}
