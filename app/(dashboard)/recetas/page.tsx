import PageHeader from "@/components/ui/page-header";import {createClient} from "@/lib/supabase/server";import RecipeCalculator from "@/components/formulas/recipe-calculator";
export default async function RecipesPage(){const supabase=await createClient();const{data}=await supabase.from("products").select("id,name,base_unit,current_cost").eq("active",true).in("inventory_mode",["PROPIO","PRODUCIDO"]).order("name");return <main className="pt-page"><PageHeader eyebrow="Calculadora" title="Recetas" description="Incluye ingredientes, mano de obra, energía, tiempos, merma y packaging para dejar de subestimar el costo de elaboración."/><RecipeCalculator products={(data??[]).map(p=>({...p,current_cost:Number(p.current_cost??0)}))}/></main>}
