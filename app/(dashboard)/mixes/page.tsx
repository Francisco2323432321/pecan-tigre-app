import PageHeader from "@/components/ui/page-header";import {createClient} from "@/lib/supabase/server";import MixCalculator from "@/components/formulas/mix-calculator";
export default async function MixesPage(){const supabase=await createClient();const{data}=await supabase.from("products").select("id,name,base_unit,current_cost").eq("active",true).eq("base_unit","g").in("inventory_mode",["PROPIO","PRODUCIDO"]).order("name");return <main className="pt-page"><PageHeader eyebrow="Calculadora" title="Mixes" description="Formula porcentajes, merma, costo por presentación y margen sin modificar stock hasta que decidas crear el producto."/><MixCalculator products={(data??[]).map(p=>({...p,current_cost:Number(p.current_cost??0)}))}/></main>}
