"use client";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
import { createClient } from "@/lib/supabase/client";
export default function UpdatePasswordPage(){const router=useRouter();const [password,setPassword]=useState("");const [repeat,setRepeat]=useState("");const [error,setError]=useState("");const [loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();if(password.length<8){setError("Usá al menos 8 caracteres.");return}if(password!==repeat){setError("Las contraseñas no coinciden.");return}setLoading(true);setError("");const {error}=await createClient().auth.updateUser({password});if(error){setError(error.message);setLoading(false);return}router.replace("/");router.refresh()}return <main className="flex min-h-dvh items-center justify-center bg-[#fff8fb] p-5"><div className="w-full max-w-[430px] rounded-[24px] border border-[#efd7e2] bg-white p-6 shadow-[0_18px_50px_rgba(91,40,63,0.08)] sm:p-8"><p className="text-xs font-extrabold tracking-[0.22em] text-[#ad416f]">PECÁN TIGRE</p><h1 className="mt-3 text-2xl font-extrabold text-[#3e2833]">Nueva contraseña</h1><form onSubmit={submit} className="mt-6 space-y-4"><label className="block"><span className="pt-label">Contraseña</span><input className="pt-input" type="password" minLength={8} required value={password} onChange={e=>setPassword(e.target.value)} autoComplete="new-password" /></label><label className="block"><span className="pt-label">Repetir contraseña</span><input className="pt-input" type="password" minLength={8} required value={repeat} onChange={e=>setRepeat(e.target.value)} autoComplete="new-password" /></label>{error&&<div className="rounded-xl bg-[#fbecef] px-3 py-2.5 text-sm font-semibold text-[#a94658]">{error}</div>}<button disabled={loading} className="pt-button-primary w-full px-4">{loading?"Guardando…":"Guardar contraseña"}</button></form></div></main>}
